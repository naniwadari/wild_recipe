<% if @recipe.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <%= @recipe.errors.count %>個のエラーがあります。
    </div>
      <ul>
        <% @recipe.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
  </div>
<% end %>